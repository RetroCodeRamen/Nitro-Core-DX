// Package memory contains the emulator memory model.
//
// Bus is the canonical memory implementation used by the emulator runtime.
// Historical duplicate memory implementations were removed to keep a single
// source of truth for banked memory, cartridge mapping, and I/O routing.
package memory
